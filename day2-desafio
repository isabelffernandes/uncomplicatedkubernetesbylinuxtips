apiVersion: v1
kind: Pod
metadata:
  name: empdir-pd
spec:
  containers:
  - image: nginx
    name: girus
    args:
    - sleep
    - "600"  
    resources:
      requests:
        memory: "64Mi"
        cpu: "0.3m"
      limits:
        memory: "128Mi"
        cpu: "0.5"
  - image: registry.k8s.io/test-webserver
    name: emp-container
    volumeMounts:
    - mountPath: /cache
      name: cache-volume
  volumes:
  - name: cache-volume
    emptyDir:
      sizeLimit: 500Mi
      medium: Memory
